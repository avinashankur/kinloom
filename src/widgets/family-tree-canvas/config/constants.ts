/**
 * Shared layout constants — must match between:
 * - genealogy-layout-engine.ts
 * - family-edge.tsx (edge path computation)
 * - union-node.tsx (rendered size)
 */
export const PERSON_W = 120;
export const PERSON_H = 90;
export const UNION_SIZE = 28; // diameter of the + union node
